import NationAPI from "./NationAPI";
import "./App.css";

function App() {
  return (
    <div>
      <NationAPI></NationAPI>
      {/* Display the Header component on every page by placing it outside Routes tag. */}
    </div>
  );
}
export default App;
